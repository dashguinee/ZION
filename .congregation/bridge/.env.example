# GitHub Configuration
GITHUB_OWNER=dashguinee
GITHUB_REPO=ZION
GITHUB_TOKEN=ghp_your_personal_access_token_here
GITHUB_BRANCH=main

# Server Configuration
BRIDGE_PORT=3001

# Service Authentication Tokens
# Generate random tokens for each AI service:
# Example: openssl rand -base64 32

CHATGPT_SERVICE_TOKEN=generate_random_token_for_chatgpt
GEMINI_SERVICE_TOKEN=generate_random_token_for_gemini
ZION_SERVICE_TOKEN=generate_random_token_for_zion

# Optional: HMAC Secret for additional security
SHARED_SECRET=generate_very_long_random_secret

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Create GitHub Personal Access Token:
#    - Go to: https://github.com/settings/tokens/new
#    - Scopes needed: repo (full access)
#    - Copy token and paste as GITHUB_TOKEN
#
# 3. Generate service tokens:
#    openssl rand -base64 32
#    (run 3 times, one for each service)
#
# 4. Install dependencies:
#    npm install
#
# 5. Start server:
#    npm start
#
# ============================================================================
